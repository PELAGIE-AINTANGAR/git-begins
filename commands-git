git clone "lien https"

git add .
git commit -m "le commentaire"
git push -u origin main

git pull

git branch "nom de la branche"
git push origin "nom de la branche"

git checkout(switcher) "nom de la branche"
